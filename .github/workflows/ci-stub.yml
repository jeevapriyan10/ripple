name: CI Stub

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

jobs:
  placeholder:
    name: placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "CI pipeline will be implemented in Module 23"
